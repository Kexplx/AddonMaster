<Controls:MetroWindow x:Class="AddonMaster.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        BorderBrush="Black"
        BorderThickness="2"
        mc:Ignorable="d"
        Title="Your Collection" TitleForeground="DarkGray" Height="400" Width="400" ResizeMode="CanMinimize"  WindowStartupLocation="CenterScreen" ShowTitleBar="False" MouseDown="Window_MouseDown" >
    <Grid>
        <Grid.Background>
            <ImageBrush Opacity="0.6" ImageSource="C:\Users\Oscar\source\repos\AddonMaster\AddonMaster.GUI\Resources\lore4.jpg" Stretch="UniformToFill"/>
        </Grid.Background>
        <ListBox BorderThickness="0" Name="lbAddonList" HorizontalContentAlignment="Stretch" Margin="0,0,0,36" Background="Transparent">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="30"/>
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="50" />
                            <ColumnDefinition Width="10" />
                            <ColumnDefinition Width="50" />
                        </Grid.ColumnDefinitions>
                        <Image Height="20" Grid.Column="0" Source="{Binding IconPath}"></Image>
                        <Label Grid.Column="1"></Label>
                        <TextBlock VerticalAlignment="Center"  Grid.Column="2" Text="{Binding Title}" />
                        <Button Click="Button_Click" Grid.Column="3">Remove</Button>
                        <Label Grid.Column="4"></Label>
                        <Button Click="Button_Click" Grid.Column="5">Update</Button>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button  Background="LightGray" Name="BtnContinue" BorderThickness="0" Click="BtnContinue_Click" Content="New Addon" HorizontalAlignment="Left" Margin="307,337,0,0" VerticalAlignment="Top" Width="73" Height="20"/>
        <Button  Background="LightGray" x:Name="BtnContinue_Copy" BorderThickness="0" Click="BtnContinue_Click" Content="Help" HorizontalAlignment="Left" Margin="10,337,0,0" VerticalAlignment="Top" Width="73" Height="20"/>
    </Grid>
</Controls:MetroWindow>
